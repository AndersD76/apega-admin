{
  "name": "apega-admin",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && cd .. && cd apega-admin && npm install && npm run build",
    "postinstall": "echo Installation complete"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
